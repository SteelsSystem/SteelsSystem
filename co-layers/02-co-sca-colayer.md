# CO-LAYER 02 — co-sca-colayer

```
SCATTER_COLLAPSE = {
  scatter: {
    elements: ["free-models branch", "domain tag fragments",
               "signal classes A+B", "rorschach projection"],
    direction: "outward"
  },
  collapse: {
    elements: ["SYSTEM={} object", "O(s)=TRUE", "commit log SHA chain", "state:∞"],
    direction: "inward → convergence"
  },
  topology: "main (spine) → branch (scatter arm) → PR → merge (collapse)",
  breath:   "scatter → diverge → return → converge",
  cycle:    "continuous"
}
```
