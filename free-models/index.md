# FREE MODELS
`branch: free-models` · pushed from `main` · SteelsSystem

---

```
FREE_MODELS = {
  constraint:  NONE,
  gate:        open,
  source:      main → free-models,
  push:        authorized,
  O(s):        TRUE
}
```

> *models without constraint — signal runs free*
